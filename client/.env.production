# Base URL of the deployed API (Render). Netlify's build reads this file
# automatically; override it in Netlify's dashboard env vars instead if the
# backend URL ever changes without editing the repo.
VITE_API_BASE_URL=https://learningmanagementsystem-astx.onrender.com
